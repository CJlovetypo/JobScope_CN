# 项目约定

本项目用于开发和管理多个求职相关的 skill。

## 目录管理

- 每个 skill 必须在项目根目录下拥有一个独立文件夹，使用清晰、能体现用途的名称。
- 每个 skill 的 `SKILL.md`、脚本、配置、参考资料及其他相关文件，统一存放在该 skill 的文件夹内。
- 运行和开发过程中产生的产物、临时文件及日志，也必须存放在对应 skill 的文件夹内，可按需建立 `artifacts/`、`tmp/`、`logs/` 等子目录。
- 新建 skill 时，先创建独立文件夹，再在其中开展工作；不要将不同 skill 的文件或产物混放，也不要将单个 skill 的文件散落在项目根目录。
- 项目根目录仅存放项目级说明、管理约定及确实由多个 skill 共用的文件。
