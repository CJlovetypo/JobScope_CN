# 工程与数据发布边界

四个功能目录 `job-search`、`job-radar`、`recruitment-link-repair`、`shared` 随 GitHub 发布。正式公司标签、描述、字段治理状态、招聘城市索引以及必要的招聘 API 配置均公开，新用户不需要维护者的私人资料即可使用这些能力。

| 位置 | 职责 | 是否公开 |
| --- | --- | --- |
| `shared/job-search-core/assets/` | 当前正式来源和采集配置 | 是 |
| `shared/job-search-core/data/company-records.json` | 正式公司画像，是日常读取入口 | 是 |
| `shared/job-search-core/data/company-{business-tags,ownership-tags,profiles,size-tags}.json` | 从同一正式画像生成的兼容数据 | 是 |
| `job-search/runtime/*/data/company-city-index.json` | 各招聘方向的正式城市索引 | 是 |
| `datasets/recruitment-links/` | 原始寻源文档、历史快照、索引和候选目录 | 否 |
| `datasets/company-research/` | 搜索原件、候选字段、复核过程、完整内部导出 | 否 |
| `docs/` | PRD、审核、真实行为模拟、过程报告 | 否 |
| `artifacts`、`runs`、`outputs`、雷达状态库 | 调试证据、个人任务及运行结果 | 否 |

## 发布与读取

维护流程主动采集和复核，在私有数据中保存完整依据。发布器按允许字段列表生成公开画像，保留正式标签、描述、供应商、时间、状态和公开来源 URL；不发布原始响应、搜索摘要、复核正文、私人笔记或本地文件路径。`api_supported` 不升级成 `verified`。

用户运行 `catalog`、`prepare`、匹配或雷达时只读取正式画像，不读取 `datasets` 和 `docs`，不触发公司标签搜索。招聘城市仍由既有岗位 API 维护链路更新。

完整独立复核记录位于 `datasets/company-research/reviews/company-label-reviews.json`；API 决策工作档案位于同目录 `company-api-labels.json.gz`；候选归档位于 `candidates/`。维护兼容输入位于 `inputs/`，通过维护工具编辑后运行：

```sh
node shared/job-search-core/scripts/publish-company-data.mjs
```

该命令发布已有结论，不联网或重新核实。研究档案重建只写私有档案；API 入库使用 `publish-api-company-labels.mjs`，独立复核发布使用 `company-records.mjs publish`，两者均更新公开正式画像与兼容文件。不要直接修改公开兼容文件作为另一份数据源。

## 模块职责与扩展边界

保留现有四个功能目录，未来产品 Skill 可以增加同级目录。产品和维护功能消费共享能力；主动维护流程发布正式数据，日常产品读取正式数据。

```text
job-search / job-radar / recruitment-link-repair / 未来新增 Skill
                         ↓
shared：共享能力、正式运行数据及数据发布入口
                         ↑ 主动维护后发布
datasets/recruitment-links / datasets/company-research
docs：设计、审核与过程报告
```

共享层不得反向依赖某个产品 Skill；产品之间通过共享接口复用能力，不直接读取对方内部文件或硬编码对方运行目录。单一产品专属能力先放在自身目录，多个产品需要且职责稳定时再提取到共享层。与招聘核心无关的共享能力可以在 `shared` 下独立成模块，无需全部放进 `job-search-core`。

根目录 `AGENTS.md` 为加载它的 Agent 提供简明开发约束，按当前规则保留本地。本公开文档承载通用规范，供项目使用者及协作者阅读。迭代方案和审核过程保存在本地 `docs`。这些指令与文档不能替代强制执行机制。

## 公司与招聘源扩充

### 新增公司主体

先核对正式主体库中是否已有该公司、别名及关联主体，再沿用现有生成方式分配 `company_id`。主体层级沿用现有契约，不因名称相近复用 ID，也不自动合并集团与子公司。

招聘寻源材料放入 `datasets/recruitment-links`，主体研究材料放入 `datasets/company-research`。按现有校验与发布流程将可用主体信息、招聘源及已支持字段纳入正式数据。公司可以先有招聘源、后补标签；缺失字段保持未知或待核，不以默认性质或业务标签填满，不阻塞其他合格字段。

### 增加或修复招聘源

为已有公司增加入口时沿用 `company_id`，按现有 `source_id` 规则区分招聘入口与渠道，不制造重复主体。原始链接和历史变化留在私有维护资料，运行所需的当前入口、API 配置与能力判定元数据进入正式库。

沿用现有主体、招聘方向、接口可用性及能力发布标准。候选链接与历史成功记录不能直接当成当前可用接口。招聘城市由岗位数据链路处理，不纳入公司背景搜索。

### 补充现有字段数据

普通数据维护仅对已定义的标签和描述字段补值、纠错或更新，沿用现有字段结构、词表、枚举和分类规则。超出这些定义的需求另行设计并按用户授权处理，不在数据补充中顺带修改。

```text
主动触发维护 → 搜索原件与候选归档 → 按现有规则逐字段判定
            → 发布正式画像及兼容数据 → 产品读取
```

按 `company_id` 关联原件与结论。主体不符、来源不明或冲突的具体字段继续待核，其他合格字段可以发布。保留供应商、来源、时间与核实状态；正式字段有值不等于独立核实，旧标签不充当新研究证据。

独立复核发布按字段合并：本次未提交的字段保留原结论、证据、核实时间及来源批次；本次显式提交的未决字段仍按未决更新。相同字段的更早结论或相同时间的冲突结论不得覆盖现有复核。历史字段不计入本轮完成数。

数据补充不改变产品入口和匹配业务规则，也不要求每次常规维护重新申请许可；在已有授权范围内执行。若公开说明中的数量或覆盖事实发生变化，推送前同步检查 README 与 GitHub About，按实际受影响内容更新。

## 新产品 Skill 接入

新增产品前，在该次设计中明确下列内容，不要求建立插件注册框架或统一调度系统：

| 项目 | 需要说明 |
| --- | --- |
| 用户需求 | 解决的问题及触发场景 |
| 输入与交付 | 必要输入和最终输出 |
| 共享能力 | 复用的模块、接口与正式数据 |
| 专属逻辑 | 仅由该 Skill 管理的流程和规则 |
| 运行数据 | 私人输入、状态、输出及缓存的保存位置 |
| 维护边界 | 是否承担维护职责，以及何时允许更新正式数据 |

沿用现有 Skill 的组织方式，在同级产品目录下放置 `SKILL.md` 及实际需要的 `scripts`、`references` 等内容，不预建无用途的空目录。共享能力放 `shared`，过程材料放私有目录。

通过共享接口消费正式公司画像与岗位能力，不复制维护另一套公司标签、招聘源或公司筛选逻辑。需要复用其他产品能力时，先确定稳定的共享接口。只服务当前产品的流程不必提前抽象成公共框架。

日常求职型 Skill 不得隐式触发公司标签搜索或更新；维护型 Skill 只在明确触发的维护任务中更新数据。正常产品使用不依赖维护者的私有资料，私人输入和运行产物继续保存在既有私有位置。

架构职责或公开使用方式发生变化时，同步修改受影响的公开说明，设计和实施记录留在 `docs`。本次规范未新增公司字段、数据契约版本机制或架构自动化检查与测试要求；已有工具与下方验证说明保持现状。

## 历史证据与恢复

公司 API 原始批次位于 `datasets/company-research/raw/`。`maintenance-paths.mjs` 将历史 `job-search/artifacts/<批次>/...` 引用映射到新目录，保留原引用文本和原件字节；迁移清单位于私有 `manifests/`。本机旧路径联接只用于过渡，公开运行不依赖它们。历史批次中的临时脚本不是受支持的新入口；必要时从旧联接路径使用 Node 的 `--preserve-symlinks --preserve-symlinks-main` 保持旧相对导入。

历史招聘链接库恢复方法见 [历史库契约](../../../recruitment-link-repair/references/history-dataset.md)。`datasets` 和 `docs` 必须另行备份，Git push 不会备份它们。仅移除当前 Git 索引不会清除旧提交中的资料；历史清理需要独立审核，不自动强推。

## 验证

在 `shared/job-search-core` 运行 `npm test` 和 `npm run check:distribution`。后者检查拟分发文件及正式画像的字段白名单，防止将私人研究资料重新加入项目。发布前还应从仅包含拟分发文件的干净副本运行测试，并验证公司倾向性搜索和城市覆盖数量一致。
