# 求职 Skill 产品需求

## 产品与现有约定

校招、实习、社招三个 skill 业务独立，共用 `shared/job-search-core` 的 API 来源、采集实现和公司事实资料。用户提供简历、求职倾向与自述，确定招聘方向、行业和城市，再确认岗位评估范围。模型完整阅读入选 JD，分别判断能力、意愿和硬性条件，交付附官方 JD 链接的四页签 Excel。

城市是用户已明确的硬条件；业务倾向通常只影响优先级。公司性质、规模与岗位匹配独立。全部已收录 API 来源保留启用，暂无岗位、证据不足、当前请求失败分别记录。不能用标题、公司名或关键词直接生成个人匹配评级。

2026-09-19 的优化前基线已提交至 GitHub：`d61562b`，包含 2,282 个招聘主体、2,871 个 API 配置。下面需求基于这套共享架构实施。

## 1. 飞书多维表格扩充来源

输入：[用户指定的表和视图](https://scnk2250sha0.feishu.cn/base/DseGb8ZeQannZ1sAp8pcLB03n7e?table=tblvEJMJ94E439wt&view=vewX9bEODr)。按该视图分页读取公司名称、招聘链接、公告链接、公司简介、业务标签、城市和岗位线索，不修改线上记录。

- 原始分页结果、记录 ID、字段结构、视图、读取时间及完整性保存到本地维护产物。申请链接和公告链接完整保留；相同 URL 去重用于验证，但保留每条原始引用。
- 表中公司名、性质、城市与介绍是线索，不直接冒充已核实事实。按公司 ID、已确认别名和真实招聘主体归并，区分集团、子公司与代理招聘。
- 先识别与现有来源相同的接口，再验证新增入口，优先 API；覆盖 ATS、自建接口、聚合招聘平台中能按雇主精确限定的公开接口。微信、附件或普通网页不能直接作为岗位 API 入库。
- 新增来源必须有真实接口响应及可追溯完整 JD；未通过的候选留在核验表，不伪造可用来源。当前只有社招／实习的来源也可收入共享库，实际岗位按方向判断。
- 原始飞书内容与维护日志保存在 skill 的 artifacts 内；共享库只发布验证后的招聘配置、事实依据和来源关系，不公开个人投递状态。

验收：逐页抓取到 `has_more=false`，记录行数、链接数、已有来源、新增可用来源和未确认原因；新增配置有公司归属、列表/详情及分页证据。

## 2. 私企、外企的厂级标签

调研并建立“求职视角的大厂／中厂／小厂”规则，不能直接把统计制度的“大型企业”、上市、融资或知名度等同于“大厂”。国家统计局的企业划型依行业使用人数、收入和资产，是规模事实的参考，求职标签须另行说明模型和适用范围。

- 先核对公司性质；私企和外企适用。国企、事业单位等标“不适用”；性质未知标“待核实”。
- 保存员工规模、统计主体、资料日期、收入或行业地位等证据；集团全球规模与在华招聘实体规模分开，子公司不自动继承集团标签。
- 规则、阈值、证据要求、置信程度及更新时间公开记录。标签必须能解释触发了哪些条件；信息不足、范围不明、相互冲突或过时则保留待核实。
- 规模属于雇主事实，不能当作薪酬、稳定性、培养质量或个人匹配度的保证。未经用户要求，不将厂级作为硬排除条件。
- 用户已确认采用“客观规模＋求职视角”。首版以可核验雇员人数划分：5,000 人及以上为大厂，500–4,999 人为中厂，不足 500 人为小厂。这是产品的组织规模模型，不是国家统计划型或行业排名。详细依据、主体口径与不确定性规则见 [规模模型](shared/job-search-core/references/company-size-model.md)。

验收：共享公司标签库覆盖所有主体，每条包含适用状态、模型版本、判定依据或缺失原因；数据不足的公司不得为了覆盖率硬打标签。

## 3. 刷新招聘城市标签

本次对共享来源中的每个公司执行地点刷新；校招、实习、社招分别保存索引。城市只取实际岗位字段及明确的工作地点，不拿总部、宣讲或面试地点替代。

- 支持全部公司及指定范围刷新、可续跑、并发与请求预算；每家公司均有本轮尝试结果。
- 当前目标岗位、地点未知、无目标岗位、请求失败及部分分页分别记录。
- 完整成功的结果替换旧标签；部分结果不得误删未覆盖的旧城市，请求失败保留旧标签并记录其陈旧状态。
- 后续默认仍使用缓存，用户主动刷新或首次初始化才请求接口；定向检索结果不能覆盖全量城市索引。

验收：三个方向均有逐公司结果与更新时间。报告分别统计新鲜完整、部分覆盖、保留旧值、无岗位和待核实，不将一次失败说成公司不在某城市招聘。

## 4. 岗位定向检索模式

新增 `targeted`，原有逐公司遍历全部可访问 JD 的方式保留为默认 `exhaustive`。只有用户明确选用快速定向模式才缩小标题检索范围。

流程：理解目标职能与业务 → 结合共享公司业务标签生成候选公司及理由 → 生成明确的岗位标题近义词与排除词 → 检索标题 → 对命中的岗位读取完整 JD → 沿用原个人匹配评估。

例如“房地产销售”可考虑开发销售、置业顾问、新房销售、房产顾问、渠道销售等词；“渠道销售”存在跨业务歧义，应结合公司业务和完整 JD 核对。关键词计划由语义理解生成并保存依据，不能把一个固定示例词表当作所有职位的推理能力。

- 用户已确定的行业、城市、招聘方向继续生效；候选公司之外的公司和关键词没有命中的岗位均列为未搜索范围。公司业务未知时保留可选候选与理由，不能声称它一定不相关。
- 每个来源记录检索能力：已实测服务器检索、参数线索待验证、列表取回后本地标题筛选。不能宣称全部配置都原生支持关键词。
- 已验证接口优先服务端关键词查询；不支持或尚未证明时读取岗位列表并筛选标题，尽可能只补取命中岗位正文。仍需全量取回的接口如实记录成本。
- 多词合并按真实岗位身份去重。服务器可能搜索全文，返回后仍做本地标题条件核对。无命中不自动扩大词表、换行业或切回全量；可向用户建议调整。
- 检索计划、公司集合、词表、检索策略进入独立缓存指纹，防止定向结果被当作全量缓存。方向、资料与画像原有指纹仍有效。
- Excel 的来源覆盖明确说明“定向检索，可能漏掉标题未体现的岗位”，展示本次搜索范围；不声称全市场或整家公司岗位全量覆盖。

验收：相同模拟目标对比两种模式，记录列表/详情请求、取得岗位和进入评估的数量；证明定向标题筛选与全文匹配分离，且不会污染全量缓存和城市索引。API 关键词能力必须有实际请求和响应，不能仅由 HTTP 200 判定筛选生效。

## 5. 招聘接口自修复

优先检查项目／站点参数轮换，其次入口及 API 网关变化，最后才判断 ATS 平台迁移。年份变化是复核线索，不能直接通过把 URL 中的 2026 替换成 2027 猜出新接口。

来源保存四层信息：稳定的公司与租户身份；入口/API 基址及方法；经常变化的 siteId、SU、projectId、batchId 等参数；当次岗位 ID、JD 链接、类型、地点与正文。历史样本中入口发现加深与真实接口迁移必须分开，不把所有修复都归为年度变更。

运行策略：

1. 调用已保存 API，记录状态、列表/详情/分页能力、数量、关键响应结构及最近成功时间。
2. 正常空列表记为暂无观察岗位，不立即宣布失效；可按明确的过期项目证据或用户主动要求进一步复核。
3. 网络瞬断、限流、登录墙、结构不符、项目失效分别归类。限流遵守等待与预算，登录或验证码不通过个人身份绕过。
4. 可修复失败时，在有限请求和时间预算内读取公开招聘入口、已有替代入口和前端配置/脚本，提取真实新参数或候选网关。优先沿用 provider 适配器。
5. 对候选重新核验雇主/租户、招聘方向、列表和 JD；不能仅凭同名、同 ATS、请求成功或疑似新 ID 自动替换。
6. 只有通过身份和接口验证的候选才自动采用，保留旧配置与证据，并安全更新共享来源。证据不足保留候选、历史配置和需人工复核的原因；不得停用来源或反复无限重试。
7. 失败计数和修复结果可续跑；新配置使旧配置方向证明失效，必须重新匹配证据。年度复核可通过维护命令主动执行，本轮不创建定时任务。

首版对公开结构明确的常用 ATS 做自动参数恢复，其余来源统一诊断和记录可复核候选。遇到新的加密协议、平台迁移或主体无法确认时明确能力边界，不承诺万能自修复。

验收：覆盖旧站点 ID、新网关、正常空列表、临时失败、身份不符、类型不符、限流和修复失败；成功修复可追溯且对三个 skill 生效，失败不得丢失历史配置、旧 JD 或城市标签。

## 发布和完成口径

旧版本先单独提交（已完成）；本轮新功能、共享数据变化、测试及文档作为后续提交。原始采集产物和个人材料继续由 `.gitignore` 排除。交付区分“功能已实现”“本轮实际验证数量”“仍待核实”，不能以模块存在代替真实来源核验或城市刷新。

## 本轮实现与验证（2026-09-19）

五项能力已落地，按上述边界执行。最终共享库存为 2,413 个主体、3,069 个配置；飞书净新增 131 个主体和 198 个配置。三个方向均完成全部公司城市刷新尝试。规模模型已按用户选择落实，证据不足保留待核实。定向检索和有限自修复已接入公共流程。

测试、采集统计、真实运行耗时及未覆盖部分见 [实现与验证记录](shared/job-search-core/references/implementation-validation-20260919.md)。
